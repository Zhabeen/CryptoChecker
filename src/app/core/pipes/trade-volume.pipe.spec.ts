import { TradeVolumePipe } from './trade-volume.pipe';

describe('TradeVolumePipe', () => {
  it('create an instance', () => {
    const pipe = new TradeVolumePipe();
    expect(pipe).toBeTruthy();
  });
});
